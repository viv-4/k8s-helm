apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rubber-soul.fullname" . }}-test-connection"
  labels:
    {{- include "rubber-soul.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rubber-soul.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
