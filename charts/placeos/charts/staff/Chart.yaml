apiVersion: v2
name: staff
description: A PlaceOS helm chart for the STAFF-API component

type: application

version: 0.1.0

# This number is reflected in the container version deployed
appVersion: placeos-1.2202.2
