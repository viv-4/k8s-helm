apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "placeos.fullname" . }}-test-connection"
  labels:
    {{- include "placeos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
  restartPolicy: Never
